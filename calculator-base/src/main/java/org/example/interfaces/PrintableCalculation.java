package org.example.interfaces;

/**
 * Represents a calculation that can be formatted into a printable string.
 */
public interface PrintableCalculation {

    /**
     * Formats this calculation as text.
     *
     * @return formatted calculation output
     */
    String format();
}